"""Build-time output.

Modules in this package are emitted by the code generator (`make codegen`) and
are deliberately not committed. An analysis run against a clean checkout sees the
package and not its contents.
"""
